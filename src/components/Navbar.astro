---
const navLinks = [
  { name: 'About', href: '/about' },
  { name: 'Books', href: '/books'},
  { name: 'World Map', href: '/map' },
];
---

<header class="fixed w-full top-0 z-50 bg-void-900/90 backdrop-blur-sm border-b border-white/10 transition-all duration-300">
  <div class="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between">
    
    <a href="/" class="text-xl md:text-2xl font-serif font-bold text-gold tracking-tighter hover:opacity-80 transition">
      S.E. DERACY
    </a>

    <nav class="hidden md:flex gap-8 items-center">
      {navLinks.map(link => (
        <a 
            href={link.href} 
            class="text-sm font-sans tracking-widest text-gray-300 hover:text-gold uppercase transition-colors"
        >
            {link.name}
        </a>
      ))}
    </nav>

    <div class="md:hidden group relative">
        <button class="text-white p-2">
             <span class="sr-only">Menu</span>
             ☰
        </button>
        <div class="absolute right-0 top-full mt-2 w-48 bg-void-900 border border-void-700 rounded shadow-xl hidden group-hover:block hover:block">
            {navLinks.map(link => (
                <a href={link.href} class="block px-4 py-3 text-white hover:bg-void-800 border-b border-void-800 last:border-0">
                    {link.name}
                </a>
            ))}
        </div>
    </div>
  </div>
</header>